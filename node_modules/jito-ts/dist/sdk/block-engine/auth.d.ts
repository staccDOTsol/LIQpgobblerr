import { Interceptor } from '@grpc/grpc-js';
import { Keypair } from '@solana/web3.js';
import { AuthServiceClient } from '../../gen/block-engine/auth';
export type AuthRefreshError = {
    reason: 'rate_limited' | 'auth_failed' | 'network_error' | 'invalid_response';
    message: string;
    retryAfter?: number;
};
export declare const authInterceptor: (authProvider: AuthProvider) => Interceptor;
export declare class Jwt {
    readonly token: string;
    private readonly expiration;
    constructor(token: string, expiration: number);
    isExpired(): boolean;
}
export declare class AuthProvider {
    private client;
    private readonly authKeypair;
    private accessToken;
    private refreshToken;
    private refreshing;
    constructor(client: AuthServiceClient, authKeypair: Keypair);
    injectAccessToken(callback: (accessToken: Jwt) => void, errorCallback?: (error: AuthRefreshError) => void): void;
    private getErrorMessage;
    private refreshAccessToken;
    static create(client: AuthServiceClient, authKeypair: Keypair): AuthProvider;
    private fullAuth;
    private static isValidToken;
}
